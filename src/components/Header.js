import React from 'react';
import style from '../style/components/header.scss'

const Header = (props) => {
    return (
        <div>
            {props.Title}
        </div>
    )
}

export default Header;
